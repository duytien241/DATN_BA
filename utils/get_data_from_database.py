from ..app.models import Restaurant, MenuItem, Address, CategoryDomain, CategoryType, Comment, District, Order, OrderDetail, PreOrder


def get_food_with_name():
    item = MenuItem.objects.filter()

    return item
